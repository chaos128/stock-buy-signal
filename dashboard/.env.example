# 대시보드(클라이언트) 환경변수. .env.local 로 복사해 채운다: cp .env.example .env.local
# 둘 다 공개(브라우저 노출)돼도 되는 값 — publishable key 는 RLS 로 보호됨.
# Vercel 배포 시엔 프로젝트 Environment Variables 에 동일하게 등록.
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
